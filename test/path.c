#include <csystem/csystem.h>
#include <stdlib.h>
#include <unity.h>
void test_path_join() {
  /*char *path = cs_path_join(NULL, "test", "/mig", "//oeret", NULL);
  TEST_ASSERT_EQUAL_STRING("test/mig/oeret", path);
  free(path);

  path = cs_path_join(NULL, "/test", "/mig", "//oeret", "max");
  TEST_ASSERT_EQUAL_STRING("/test/mig/oeret/max", path);
  free(path);

  path = cs_path_join(NULL, "/test", "/mig", "../oeret", "max");
  TEST_ASSERT_EQUAL_STRING("/test/oeret/max", path);
  free(path);

  path = cs_path_join(NULL, "/test", "/mig", "../../oeret", "max");
  TEST_ASSERT_EQUAL_STRING("oeret/max", path);
  free(path);

  path = cs_path_join_array(
      NULL, (const char *[]){"/test", "/mig", "../../oeret", "max", NULL});
  TEST_ASSERT_EQUAL_STRING("oeret/max", path);
  free(path);*/
}